# Base URL of the backend server (no trailing slash, no /api suffix — the
# app appends /api itself). Must exactly match one of the backend's
# CLIENT_URLS-allowed origins for CORS/cookies to work.
REACT_APP_API_URL=http://localhost:5000
